<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 1. The Background Track (Empty Part) -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="2dp" />
            <solid android:color="#40FFFFFF" /> <!-- Semi-transparent White -->
        </shape>
    </item>

    <!-- 2. The Progress Fill (Filled Part) -->
    <item android:id="@android:id/progress">
        <!--
             Clip ensures the color fills from bottom to top.
             Clip orientation is vertical, gravity is bottom.
        -->
        <clip
            android:clipOrientation="vertical"
            android:gravity="bottom">
            <shape>
                <corners android:radius="2dp" />
                <solid android:color="@color/white" />
            </shape>
        </clip>
    </item>

</layer-list>