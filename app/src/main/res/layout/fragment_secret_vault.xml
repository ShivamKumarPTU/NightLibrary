<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/coordinatorLayout_vault"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_primary"
    tools:context=".SecretVaultFragment">

    <LinearLayout
        android:id="@+id/mainContainer_vault"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Header Section (This part is correct) -->
        <LinearLayout
            android:id="@+id/header_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="24dp"
            android:paddingTop="24dp"
            android:paddingEnd="24dp"
            android:paddingBottom="16dp">

            <ImageView
                android:id="@+id/header_icon"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@drawable/ic_download"
                app:tint="@color/text_primary"
                tools:ignore="ContentDescription" />

            <LinearLayout
                android:id="@+id/header_text_container"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/header_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Private Vault"
                    android:textColor="@color/text_primary"
                    android:textSize="24sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/header_subtitle_combined"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:text="Secure storage"
                    android:textColor="@color/text_secondary"
                    android:textSize="14sp" />
            </LinearLayout>
        </LinearLayout>

        <!-- Main Category Tabs (TabItems should not have children) -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout_categories"
            style="@style/VaultPrimaryTabLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <com.google.android.material.tabs.TabItem
                android:layout_height="wrap_content"
                android:layout_width="wrap_content"
                android:icon="@drawable/ic_media"
                android:text="Media" />

            <com.google.android.material.tabs.TabItem
                android:layout_height="wrap_content"
                android:layout_width="wrap_content"
                android:icon="@drawable/lockicon"
                android:text="Passwords" />

            <com.google.android.material.tabs.TabItem
                android:layout_height="wrap_content"
                android:layout_width="wrap_content"
                android:icon="@drawable/ic_contacts"
                android:text="Contacts" />

        </com.google.android.material.tabs.TabLayout>

        <!-- THIS IS THE KEY PART: A single FrameLayout to hold the swapping content -->
        <FrameLayout
            android:id="@+id/content_frame"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:animateLayoutChanges="true">

            <!-- 1. Media Content (Visible only for Media tab) -->
            <LinearLayout
                android:id="@+id/media_content_layout"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:visibility="gone"
                tools:visibility="visible">

                <com.google.android.material.tabs.TabLayout
                    android:id="@+id/tabLayout_status"
                    style="@style/SecondaryTabLayout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="16dp"
                    android:layout_marginEnd="16dp"
                    android:layout_marginTop="8dp"
                    app:tabGravity="fill"
                    app:tabMode="fixed">

                    <com.google.android.material.tabs.TabItem
                        android:layout_height="wrap_content"
                        android:layout_width="wrap_content"
                        android:text="In Progress" />

                    <com.google.android.material.tabs.TabItem
                        android:layout_height="wrap_content"
                        android:layout_width="wrap_content"
                        android:text="Completed" />
                </com.google.android.material.tabs.TabLayout>

                <androidx.viewpager2.widget.ViewPager2
                    android:id="@+id/view_pager_vault"
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:layout_marginTop="8dp"
                    android:layout_weight="1" />
            </LinearLayout>

            <!-- 2. Passwords Summary Card (Now correctly placed inside the FrameLayout) -->
            <include
                android:id="@+id/password_summary_card"
                layout="@layout/layout_summary_card"
                android:visibility="gone" />

            <!-- 3. Contacts Summary Card (Now correctly placed inside the FrameLayout) -->
            <include
                android:id="@+id/contacts_summary_card"
                layout="@layout/layout_summary_card"
                android:visibility="gone" />

        </FrameLayout>

    </LinearLayout>

    <!-- Floating Action Button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:contentDescription="Add New Item"
        android:src="@drawable/ic_add1"
        app:backgroundTint="@color/bg_card_alt"
        app:elevation="0dp"
        app:shapeAppearanceOverlay="@style/FabShape"
        app:tint="@color/text_secondary" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>